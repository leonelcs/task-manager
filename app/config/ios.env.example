# iOS Mobile App Configuration
# Add these to your .env file for iOS OAuth support

# iOS Google OAuth Client ID from config.plist
GOOGLE_IOS_CLIENT_ID=813648694576-i7ajuil729ci26n7q40fds1ffus1moo0.apps.googleusercontent.com

# iOS Bundle ID from config.plist  
GOOGLE_IOS_BUNDLE_ID=com.engandmore.AdhdTaskManager

# Note: The iOS client ID is already public in the mobile app bundle,
# so it's safe to include in documentation. However, never expose
# client secrets in mobile apps.
