# Local Development Environment
ENVIRONMENT=local

# App Configuration
DEBUG=true
APP_NAME=ADHD Task Manager API (Local)
APP_VERSION=1.0.0

# Database Configuration - MySQL for local development
DATABASE_URL=mysql+pymysql://adhd_tasksmanager:My:S3cr3t@localhost:30306/adhd_tasks_dev
DB_POOL_SIZE=3
DB_MAX_OVERFLOW=5

# Alternative: SQLite for simple testing (comment out MySQL above to use this)
# DATABASE_URL=sqlite:///./adhd_tasks.db

# Security Configuration
SECRET_KEY=local-dev-secret-key-not-for-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS Configuration - Allow all for local development
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://localhost:8080

# AI/ML Configuration
OPENAI_API_KEY=your-openai-api-key-here
AI_MODEL=gpt-3.5-turbo

# Email Configuration (Optional for local)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Logging
LOG_LEVEL=DEBUG
